Public Class Worker
    Dim Skillset() As Skill 'Array of Skills a worker has
    Dim WorkerQuality As Integer 'Quality of Worker's Work
    Dim WorkerPayment As Integer 'Payment They Require
    Dim WorkerSpeed As Integer   'Speed of Working
    Public Sub New()
        'procedural Worker Generation Code
    End Sub
End Class
